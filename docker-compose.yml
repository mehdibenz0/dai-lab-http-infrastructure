version: '3'

services:
  api-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - static-server

  static-server:
    build:
      context: ./static-server
      dockerfile: Dockerfile
    ports:
      - "80:80"
